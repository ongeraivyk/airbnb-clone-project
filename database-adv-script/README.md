# Database Advanced SQL Scripts

This directory contains SQL solutions for advanced database operations required by the ALX Airbnb backend project.

---

## ✅ Task 0: Complex SQL Joins

### 🔹 INNER JOIN — Bookings with Users
Returns only bookings that have a matching user.

### 🔹 LEFT JOIN — Properties with Reviews
Returns all properties, including those without reviews.

### 🔹 FULL OUTER JOIN — Users and Bookings
Returns:
- all users (even without bookings)
- all bookings (even without a user)
